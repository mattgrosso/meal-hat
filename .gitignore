.DS_Store
node_modules
/dist
.env
invalidation_output.txt

# Database snapshots written by scripts/reset-fridge.mjs — real household data,
# and the only copy of what a reset deleted. Keep them on disk, out of git.
/backups

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.history
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local permissions (machine-specific)
.claude/settings.local.json
